{
  "name": "recruitly",
  "private": true,
  "scripts": {
    "dev": "npx concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "start": "npx concurrently \"cd backend && npm start\" \"cd frontend && npx vite --host\""
  }
}
